variant: fcos
version: 1.5.0

passwd:
  users:
    - name: lab
      groups: ["wheel"]
      shell: /usr/bin/bash
      password_hash: "$6$35ODAdJ9yQq6SZKM$vefL39du/eIsWYsUDz2LJzl9PYFOAbEhIlWrOYQ0yP/9BZU3PJITjDvIwa5SYykFThxzfon0qCU7num5pPm6e."

systemd:
  units:
    - name: sshd.service
      enabled: true
      dropins:
        - name: 10-password-auth.conf
          contents: |
            [Service]
            ExecStartPre=/usr/bin/mkdir -p /etc/ssh/sshd_config.d
            ExecStartPre=/usr/bin/bash -c 'echo "PasswordAuthentication yes" > /etc/ssh/sshd_config.d/20-password-auth.conf'

    - name: k3s-install.service
      enabled: true
      contents: |
        [Unit]
        Description=Install and start k3s (single node)
        Wants=network-online.target
        After=network-online.target

        [Service]
        Type=oneshot
        ExecStart=/opt/k3s-install.sh --write-kubeconfig-mode 644 --disable traefik
        StandardOutput=append:/var/log/k3s-install.log
        StandardError=append:/var/log/k3s-install.log
        RemainAfterExit=yes

        [Install]
        WantedBy=multi-user.target

storage:
  files:
    - path: /etc/hostname
      mode: 0644
      contents:
        inline: "homelab"

    - path: /opt/k3s-install.sh
      mode: 0755
      contents:
        source: https://get.k3s.io
